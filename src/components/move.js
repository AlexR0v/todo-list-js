const  move = ()=>{

	const todoList = document.querySelector('.todo-list');

	todoList.addEventListener('mousedown', ({target})=>{
		if(target.classList.contains('list-task')){

		}
	})

}
export default move;